import React from 'react'
import NavBar from './navbar'

const Header = () => (
  <header className="global-header header-bar">
    <h1 className="site-title">Unify</h1>

    <NavBar />
  </header>
)

export default Header
